<h1>All posts</h1>
<% @posts.each do |post| %>
  <div class="alert alert-light">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Next", post_path(post), :class => 'btn btn-outline-primary' %>
  </div>
<% end %>